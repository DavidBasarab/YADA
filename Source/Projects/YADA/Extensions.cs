using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;

namespace YADA
{
    internal static class Extensions
    {
        //public static T GetValue<T>(this IDataReader reader, string columnName)
        //{
        //    if (reader..ColumnNameNotFound(columnName) || row.Table.Columns[columnName] == null || row[columnName] is DBNull)
        //    {
        //        return default(T);
        //    }

        //    return (T)row[columnName];
        //}
    }
}
